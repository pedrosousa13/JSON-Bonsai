{
  "name": "json-bonsai",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && vite build --watch --mode content",
    "build": "vite build --mode content && vite build --mode page-script && vite build --mode tree-worker",
    "typecheck": "tsc --noEmit",
    "zip": "npm run build && node ./scripts/zip-dist.mjs",
    "package": "npm run build && node ./scripts/zip-dist.mjs json-bonsai-chrome.zip && node ./scripts/zip-dist.mjs json-bonsai-firefox.zip",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.39",
    "@types/jmespath": "^0.15.2",
    "@types/node": "^25.5.2",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.0",
    "vite": "^6.2.0",
    "vite-plugin-static-copy": "^3.0.0",
    "vitest": "^3.2.4",
    "yazl": "^3.3.1"
  },
  "dependencies": {
    "jmespath": "^0.16.0"
  }
}
